:host {
  display: block;
}

/* 评论-顶部 */
.comment-header {
  font-size: 32rpx;
  color: var(--main);
  margin: 0 20rpx;
  line-height: 100rpx;
  display: flex;
  align-items: center;
}

/* 评论-图标 */
.comment-icon {
  width: 32rpx;
  height: 32rpx;
  margin-right: 10rpx;
  display: block;
}